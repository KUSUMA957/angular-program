import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';
@Component({
  selector: 'app-dashboard-settings',
  standalone: true,
  imports: [RouterModule], 
  template: `<h3>Settings</h3><p>Settings content...</p>`
})
export class SettingsComponent {}