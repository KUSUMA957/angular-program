import { StudentMarksFilterPipe } from './student-marks-filter-pipe';

describe('StudentMarksFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StudentMarksFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
